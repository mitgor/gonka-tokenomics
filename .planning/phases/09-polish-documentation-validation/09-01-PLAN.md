---
phase: 09-polish-documentation-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - models/parameters.py
  - generators/workbook_base.py
autonomous: true

must_haves:
  truths:
    - "Every parameter in PARAM_GROUPS has a 'confidence' field with value HIGH, MED, or LOW"
    - "Every parameter's 'source' field contains a full document reference (e.g., 'Fine-Tuning Recs, Rec #3' not just 'Rec #3')"
    - "The Assumptions tab displays column E with confidence levels for every parameter row"
    - "Column E header merge extends to include the confidence column (A1:F1 merge, A2:F2 merge)"
    - "Column D width is 40 to accommodate longer source citations"
  artifacts:
    - path: "models/parameters.py"
      provides: "All 60 parameters with enriched 'source' fields and new 'confidence' fields"
      contains: "confidence"
    - path: "generators/workbook_base.py"
      provides: "Column E (Confidence) writer in both build_assumptions_tab and build_filtered_assumptions_tab"
      contains: "confidence"
  key_links:
    - from: "generators/workbook_base.py"
      to: "models/parameters.py"
      via: "param.get('confidence', '') reads the new field from each parameter dict"
      pattern: "confidence"
    - from: "generators/workbook_base.py"
      to: "generators/styles.py"
      via: "_SOURCE_FONT used for confidence column styling (same italic as source column)"
      pattern: "_SOURCE_FONT"
---

<objective>
Enrich all 60 parameters with full document-reference source citations and confidence levels (HIGH/MED/LOW), and update both Assumptions tab builders to display the new Confidence column E.

Purpose: Satisfies REQ-U10 (source references per assumption) and REQ-D04 (assumption audit trail with source + confidence). This is the data-layer enrichment that all workbooks inherit through PARAM_GROUPS.

Output: Enhanced `parameters.py` with 60 enriched source fields + 60 new confidence fields. Updated `workbook_base.py` writing column E on every Assumptions tab.
</objective>

<execution_context>
@/Users/mit/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mit/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-polish-documentation-validation/09-RESEARCH.md
@models/parameters.py
@generators/workbook_base.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enrich parameters.py with full source citations and confidence levels</name>
  <files>models/parameters.py</files>
  <action>
Add a "confidence" field to every parameter dict in PARAM_GROUPS. Also enhance the "source" field with full document references. Use these exact mappings:

**Source document abbreviation mapping:**
| Current Source | Enhanced Source |
|----------------|----------------|
| "Whitepaper" | "Whitepaper" (keep as-is -- already clear) |
| "Whitepaper (68%)" | "Whitepaper, 68% allocation" |
| "Whitepaper (12%)" | "Whitepaper, 12% allocation" |
| "Whitepaper (20%)" | "Whitepaper, 20% allocation" |
| "Rec #N" | "Fine-Tuning Recs, Rec #N" |
| "Rec #N (description)" | "Fine-Tuning Recs, Rec #N (description)" |
| "v1.0 Research" | "Macro Research, price scenarios" |
| "Network Data" | "Network Data, Q1 2026" |
| "Assumption" | "Assumption" (keep as-is) |
| "Research" | "Deep Analysis, GPU deflation" |
| "Strategic Data" | "Deep Analysis, Bitfury analysis" |
| "Industry" | "Industry benchmark" |
| "Market Data" | "Market Data, Q1 2026" |
| "Lambda Labs Q1 2026" | "Lambda Labs, on-demand Q1 2026" |
| "CoreWeave 3yr reserved Q1 2026" | "CoreWeave, 3yr reserved Q1 2026" |
| "H100 inference avg" | "H100 specs, inference avg" |
| "Placeholder (Phase 4 replaces)" | "Placeholder" |
| "Placeholder (leadership adjustable)" | "Placeholder" |
| "Scenario input" | "Scenario input" (keep as-is) |
| "Toggle" | "Toggle" (keep as-is) |

**Confidence level assignment:**
- **HIGH** (42 params): All "Whitepaper" sourced, all "Rec #N" sourced, all "Network Data" sourced
- **MED** (12 params): "v1.0 Research" (6 price scenarios), "Industry" (3 electricity costs), "Market Data" (2 hardware costs), "Research" (GPU deflation)
  - Also MED: "Strategic Data" (Bitfury), "Lambda Labs" and "CoreWeave" (rental rates), "H100 inference avg" (GPU power)
- **LOW** (6 params): "Assumption" (Epochs Per Day), "Placeholder" (2 params), "Scenario input" (1 param), "Toggle" (2 params)

Total: 60 parameters. Verify the count matches after edits.

Do NOT change any parameter `value`, `unit`, `name`, or `format` fields. Only modify `source` and add `confidence`.

Example of the final structure for one parameter:
```python
{
    "name": "Host Share",
    "value": 0.70,
    "unit": "",
    "source": "Fine-Tuning Recs, Rec #3",
    "confidence": "HIGH",
    "format": "percent",
},
```
  </action>
  <verify>
Run `python -c "from models.parameters import PARAM_GROUPS, get_all_params; params = get_all_params(); assert len(params) == 60, f'Expected 60, got {len(params)}'; assert all('confidence' in p for p in params), 'Missing confidence'; levels = [p['confidence'] for p in params]; print(f'HIGH={levels.count(\"HIGH\")}, MED={levels.count(\"MED\")}, LOW={levels.count(\"LOW\")}'); assert all(p['confidence'] in ('HIGH', 'MED', 'LOW') for p in params)"` passes with reasonable distribution.
  </verify>
  <done>All 60 parameters have enriched source citations and valid confidence levels (HIGH/MED/LOW).</done>
</task>

<task type="auto">
  <name>Task 2: Add Confidence column E to both Assumptions tab builders</name>
  <files>generators/workbook_base.py</files>
  <action>
Modify `workbook_base.py` to write column E (Confidence) on the Assumptions tab. Changes needed:

1. **Update `_COL_WIDTHS`** to add column E:
```python
_COL_WIDTHS = {
    "A": 35,
    "B": 20,
    "C": 15,
    "D": 40,   # Wider for full citations (was 30)
    "E": 12,   # NEW: Confidence column
}
```

2. **Update header merges in both functions** from `A1:E1` to `A1:F1` and `A2:E2` to `A2:F2`:
   - In `build_assumptions_tab()`: Lines with `ws.merge_cells("A1:E1")` and `ws.merge_cells("A2:E2")`
   - In `build_filtered_assumptions_tab()`: Same merges

3. **Update section header merges in both functions** from `end_column=4` to `end_column=5`:
   - In `build_assumptions_tab()`: The `ws.merge_cells(start_row=current_row, start_column=1, end_row=current_row, end_column=4)` call
   - In `build_filtered_assumptions_tab()`: Same call

4. **Add column E writer in `build_assumptions_tab()`** -- in the parameter loop, after the column D source cell write, add:
```python
# Column E: Confidence level (locked, italic)
confidence_cell = ws.cell(row=current_row, column=5, value=param.get("confidence", ""))
confidence_cell.font = _SOURCE_FONT
```

5. **Add column E writer in `build_filtered_assumptions_tab()`** -- same addition in its parameter loop.

6. **Update scenario selector section header merge** in `_add_scenario_selector()`: Change `end_column=4` to `end_column=5` in the merge_cells call.

Do NOT change any other logic. The `param_refs` dict, Protection(locked=False), DataValidation, freeze_panes, and all other behavior stay exactly the same.
  </action>
  <verify>
Run `python -c "
from generators.workbook_base import create_workbook
wb, refs = create_workbook()
ws = wb['Assumptions']
# Check column E exists with confidence data
found = False
for row in ws.iter_rows(min_col=5, max_col=5, min_row=4, max_row=120):
    for cell in row:
        if cell.value in ('HIGH', 'MED', 'LOW'):
            found = True
            break
    if found:
        break
assert found, 'No confidence values found in column E'
# Check merge extends to F
from openpyxl.utils import range_boundaries
merges = [str(m) for m in ws.merged_cells.ranges]
assert any('F1' in m for m in merges), f'A1:F1 merge not found in {merges}'
print('Column E + merges OK')
"` passes.
  </verify>
  <done>Both build_assumptions_tab() and build_filtered_assumptions_tab() write column E (Confidence) with italic font, merges extend to column F, column D width increased to 40.</done>
</task>

</tasks>

<verification>
1. `python -c "from models.parameters import get_all_params; p=get_all_params(); print(len(p)); assert all('confidence' in x for x in p)"` -- confirms 60 params with confidence
2. `python generate.py --test` -- generates test workbook without errors
3. Open `output/gonka_test_assumptions.xlsx` and confirm column E shows HIGH/MED/LOW values
</verification>

<success_criteria>
- All 60 parameters have enriched `source` field with full document references
- All 60 parameters have `confidence` field with HIGH, MED, or LOW
- Column E displays in both master and standalone Assumptions tabs
- No existing functionality broken (param_refs, Protection, DataValidation all unchanged)
</success_criteria>

<output>
After completion, create `.planning/phases/09-polish-documentation-validation/09-01-SUMMARY.md`
</output>
