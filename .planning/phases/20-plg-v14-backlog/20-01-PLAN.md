---
phase: 20-plg-v14-backlog
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - output/gonka_plg_growth_model.md
autonomous: true
requirements: [GTM-03]

must_haves:
  truths:
    - "PLG funnel has named stages from first touch to paid conversion with target conversion rates"
    - "Free tier spec defines email-only signup with concrete usage limits and upgrade triggers"
    - "Time-to-first-inference plan targets under 5 minutes with numbered steps"
    - "Every claim references a specific Phase 15-19 output document"
  artifacts:
    - path: "output/gonka_plg_growth_model.md"
      provides: "PLG funnel, free tier design, time-to-first-inference plan"
      contains: "## PLG Funnel Model"
  key_links:
    - from: "output/gonka_plg_growth_model.md"
      to: "output/gonka_developer_personas.md"
      via: "persona-specific conversion drivers"
      pattern: "Weekend Builder|Startup CTO|Privacy-First"
    - from: "output/gonka_plg_growth_model.md"
      to: "output/gonka_agent_pricing_analysis.md"
      via: "free tier limits grounded in pricing analysis"
      pattern: "Scenario B|\\$0\\.35"
---

<objective>
Create the PLG growth model document containing the self-serve funnel with conversion rates, free tier design spec (email-only signup), and time-to-first-inference plan (under 5 minutes).

Purpose: This is the first half of the v1.3 capstone -- synthesizing Phase 15-19 research into an actionable product-led growth model that tells Gonka exactly how developers will discover, try, adopt, and pay for the platform.

Output: `output/gonka_plg_growth_model.md`
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-plg-v14-backlog/20-CONTEXT.md
@.planning/research/STACK.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: PLG funnel model and free tier design spec</name>
  <files>output/gonka_plg_growth_model.md</files>
  <read_first>
    - output/gonka_developer_personas.md (AAARRRP journey maps, 3 personas, adoption triggers)
    - output/gonka_agent_pricing_analysis.md (Scenario B pricing, per-task costs, monthly projections)
    - output/gonka_competitive_feature_matrix.md (competitor free tiers, feature gaps)
    - output/gonka_provider_landscape_map.md (must-close gaps, competitive positioning)
    - output/gonka_channel_strategy.md (P0 channels, API-active developer KPI)
    - output/gonka_message_house.md (core positioning, vocabulary guidelines, never-say list)
  </read_first>
  <action>
Create `output/gonka_plg_growth_model.md` with three major sections. Target 400-600 lines total across Tasks 1 and 2.

**Section 1: PLG Funnel Model**

Define 6 funnel stages mapped to AAARRRP from Phase 16:

| Stage | AAARRRP | Definition | Target Rate | Metric |
|-------|---------|------------|-------------|--------|
| Discover | Awareness | Developer encounters Gonka (OpenClaw Provider Directory, GitHub, Discord) | -- | Unique visitors |
| Explore | Acquisition | Developer visits docs/landing page | 40-50% of Discover | Page views |
| Sign Up | Acquisition | Email-only API key creation | 15-25% of Explore | Signups |
| First Inference | Activation | First successful API call | 70-80% of Sign Up | API calls |
| Habitual Use | Retention | 100+ API calls in 30 days | 20-30% of First Inference | API-active devs (P18 KPI) |
| Paid Conversion | Revenue | Exceeds free tier, upgrades | 5-10% of Habitual Use | Paying users |

For each stage:
- Name the primary conversion driver per persona (Weekend Builder, Startup CTO, Privacy-First)
- Identify the #1 drop-off risk with mitigation from Phase 15-19 research
- Reference specific Phase output documents for every claim

Include a "Funnel Math" subsection with example numbers: if 1,000 developers discover Gonka, how many become paying users at target rates? Show range (pessimistic/target/optimistic).

**Section 2: Free Tier Design Spec**

Per CONTEXT.md locked decision: email-only signup, no wallet/crypto.

Define:
- **Signup flow**: Email -> verification link -> API key displayed -> copy to openclaw.json
- **Usage limits**: Ground in Phase 15 competitive analysis of competitor free tiers. Propose specific limits (e.g., X requests/day, Y tokens/month). Limits should be generous enough for Weekend Builder's typical usage but trigger upgrade for Startup CTO's production workload.
- **Rate limiting**: Requests per minute cap that prevents abuse but allows burst testing
- **Upgrade triggers**: 3 specific triggers (usage limit approached, session limit, model tier access) with in-API notification mechanism (X-Gonka-Usage-Remaining header)
- **What's NOT in free tier**: Features reserved for paid (e.g., SLA, priority routing, higher rate limits)

Reference Scenario B pricing ($0.35/$1.75 per Mtok) from Phase 15 pricing analysis for paid tier positioning.

Cross-reference never-say list from Phase 17 -- ensure all copy avoids the 16 banned crypto terms.
  </action>
  <verify>
    grep -c "## PLG Funnel Model" output/gonka_plg_growth_model.md
    grep -c "## Free Tier Design" output/gonka_plg_growth_model.md
    grep -c "Email-only" output/gonka_plg_growth_model.md
    grep -c "Weekend Builder\|Startup CTO\|Privacy-First" output/gonka_plg_growth_model.md
  </verify>
  <acceptance_criteria>
    - grep "## PLG Funnel Model" output/gonka_plg_growth_model.md returns match
    - grep "## Free Tier Design" output/gonka_plg_growth_model.md returns match
    - grep "Email-only" output/gonka_plg_growth_model.md returns match
    - grep -c "Weekend Builder\|Startup CTO\|Privacy-First" output/gonka_plg_growth_model.md returns 6+
    - grep "conversion" output/gonka_plg_growth_model.md returns matches (funnel has conversion rates)
    - grep "Scenario B\|\\$0.35" output/gonka_plg_growth_model.md returns match (pricing grounded)
  </acceptance_criteria>
  <done>PLG funnel has 6 named stages with target conversion rates per persona, funnel math with range projections, and free tier spec with email-only signup, concrete usage limits, upgrade triggers, and competitive grounding</done>
</task>

<task type="auto">
  <name>Task 2: Time-to-first-inference plan</name>
  <files>output/gonka_plg_growth_model.md</files>
  <read_first>
    - .planning/research/STACK.md (OpenClaw openclaw.json config snippet format)
    - output/gonka_partnership_playbook.md (ClawHub submission, provider plugin requirements)
  </read_first>
  <action>
Append Section 3 to `output/gonka_plg_growth_model.md`:

**Section 3: Time-to-First-Inference Plan**

Per CONTEXT.md locked decision: target under 5 minutes from email to first API response.

Define the exact steps with time budget per step:

| Step | Action | Time Budget | Blocker Risk |
|------|--------|-------------|--------------|
| 1 | Land on gonka.ai | 0s | Page must exist (must-close gap from P15) |
| 2 | Click "Get API Key" | 10s | Single CTA, no distracting navigation |
| 3 | Enter email, submit | 30s | Email-only, no wallet/OAuth complexity |
| 4 | Receive verification email | 60s | Must be instant; use transactional email service |
| 5 | Click link, see API key | 15s | Key displayed with one-click copy button |
| 6 | Paste into openclaw.json | 90s | Show exact config snippet from STACK.md |
| 7 | Send first message to agent | 60s | Agent responds via Gonka inference |
| Total | | ~4 min 15s | Under 5-minute target |

For each step:
- Show the exact UI/UX element or CLI command
- Identify what must be built (cross-reference to engineering backlog in Plan 02)
- Flag current gaps (no docs site, no signup flow per Phase 15 must-close gaps)

Include subsection "The Atomic Growth Unit" explaining why the openclaw.json config snippet is the viral mechanism: one developer configures Gonka, shares their config, other developers copy it.

Include subsection "What Competitors Require" comparing Gonka's target flow against OpenRouter (GitHub OAuth + credit card) and Together AI (OAuth + credit card + model selection) to show Gonka's friction advantage.

End the document with a "Cross-References" section listing every Phase 15-19 output document cited and what was drawn from each.
  </action>
  <verify>
    grep -c "## Time-to-First-Inference" output/gonka_plg_growth_model.md
    grep "Under 5" output/gonka_plg_growth_model.md
    grep "openclaw.json" output/gonka_plg_growth_model.md
    grep "Atomic Growth Unit" output/gonka_plg_growth_model.md
    wc -l output/gonka_plg_growth_model.md
  </verify>
  <acceptance_criteria>
    - grep "## Time-to-First-Inference" output/gonka_plg_growth_model.md returns match
    - grep "Under 5\|under 5\|< 5\|4 min" output/gonka_plg_growth_model.md returns match
    - grep "openclaw.json" output/gonka_plg_growth_model.md returns match
    - grep "Atomic Growth Unit" output/gonka_plg_growth_model.md returns match
    - grep "Cross-References\|Cross-references" output/gonka_plg_growth_model.md returns match
    - wc -l output/gonka_plg_growth_model.md shows 350+ lines
  </acceptance_criteria>
  <done>Time-to-first-inference plan has numbered steps with time budgets totaling under 5 minutes, atomic growth unit explanation, competitor comparison, and cross-references to all source documents</done>
</task>

</tasks>

<verification>
- `output/gonka_plg_growth_model.md` exists with 3 major sections
- PLG funnel has 6 stages with conversion rates and per-persona drivers
- Free tier spec is email-only with concrete limits and upgrade triggers
- Time-to-first-inference plan targets under 5 minutes with step-by-step breakdown
- All claims reference Phase 15-19 output documents
- No crypto jargon from Phase 17 never-say list appears in developer-facing copy
</verification>

<success_criteria>
- Document contains PLG funnel, free tier design, and time-to-first-inference sections
- Every funnel stage has a target conversion rate and per-persona driver
- Free tier has specific usage limits grounded in competitive analysis
- Time-to-first-inference steps total under 5 minutes
- 350+ lines of synthesized, evidence-grounded content
</success_criteria>

<output>
After completion, create `.planning/phases/20-plg-v14-backlog/20-01-SUMMARY.md`
</output>
